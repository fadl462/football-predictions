'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Register(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);const r=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,password})});const j=await r.json();if(!r.ok){setErr(j.error||'Unable to register');setLoading(false);return}router.push('/pricing');router.refresh()}return <main className="page"><div className="formWrap"><div className="form"><span className="eyebrow">Join Football Edge</span><h1 style={{fontSize:34,marginTop:18}}>Create account</h1>{err&&<div className="notice" style={{marginBottom:12}}>{err}</div>}<form onSubmit={submit}><label className="label">Name<input className="input" required value={name} onChange={e=>setName(e.target.value)}/></label><label className="label">Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label">Password<input className="input" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn primary" style={{width:'100%'}} disabled={loading}>{loading?'Creating…':'Create account'}</button></form><p className="muted" style={{fontSize:12,marginTop:18}}>Already registered? <Link href="/login" style={{color:'var(--green)'}}>Sign in</Link></p></div></div></main>}

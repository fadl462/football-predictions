import Link from 'next/link'; import {homeData} from '@/lib/data'; import {formatAlgeriaTime} from '@/lib/config';
export default async function Matches(){const {matches}=await homeData();return <main className="page"><div className="container"><div className="sectionHead"><div><span className="eyebrow">MATCH CENTRE</span><h1>Every fixture. <span>Ranked by importance.</span></h1><p className="muted">Europe Elite → UEFA → Global → Algeria. Open any match for its prediction context.</p></div><Link className="btn primary" href="/">Today's desk</Link></div><div className="filterRow"><span className="filter active">All</span><span className="filter">Europe Elite</span><span className="filter">UEFA</span><span className="filter">Global</span><span className="filter">Algeria</span></div><div className="fixtureList" style={{marginTop:20}}>{matches.map((m:any)=><Link href={`/matches/${m.id}`} className="fixture matchLink" key={m.id}><div className="league"><b>{m.league}</b><br/><span>{m.country}</span></div><div className="teamsLine"><b>{m.home}</b> <span className="muted">vs</span> <b>{m.away}</b></div><div><div className="kick">{formatAlgeriaTime(m.kickoff)}</div><span className="status">{m.status==='NS'?'UPCOMING':m.status}</span></div><span className="arrow">→</span></Link>)}</div></div></main>}

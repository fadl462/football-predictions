import {homeData} from '@/lib/data';
export default async function Matches(){const {matches}=await homeData();return <main className="page"><div className="container"><span className="eyebrow">Football data</span><h1>Matches</h1><p className="muted">Fixtures, leagues and kickoff times synced from the football data layer.</p><div className="fixtureList" style={{marginTop:28}}>{matches.map((m:any)=><div className="fixture" key={m.id}><div className="league">{m.league}<br/><span>{m.country}</span></div><div className="teamsLine">{m.home} <span className="muted">vs</span> {m.away}</div><div><div className="kick">{new Date(m.kickoff).toLocaleString([], {hour:'2-digit',minute:'2-digit'})}</div><span className="status">{m.status==='NS'?'UPCOMING':m.status}</span></div></div>)}</div></div></main>}

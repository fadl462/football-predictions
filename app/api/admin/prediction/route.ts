import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/auth';import {prisma} from '@/lib/prisma';
export async function POST(req:Request){if(!await requireAdmin())return new NextResponse('Forbidden',{status:403});const {id,selection,market,confidence,status}=await req.json();if(!id)return NextResponse.json({error:'Prediction id required'},{status:400});const p=await prisma.prediction.update({where:{id},data:{...(selection?{selection}:{}),...(market?{market}:{}),...(confidence?{confidence:Number(confidence)}:{}),...(status?{status}:{}),manuallyOverridden:true}});return NextResponse.json(p)}

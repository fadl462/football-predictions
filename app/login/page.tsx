'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);setErr('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok){setErr(j.error||'Unable to sign in');setLoading(false);return}router.push(j.role==='ADMIN'?'/admin':'/vip');router.refresh()}return <main className="page"><div className="formWrap"><div className="form"><span className="eyebrow">Welcome back</span><h1 style={{fontSize:34,marginTop:18}}>Sign in</h1>{err&&<div className="notice" style={{borderColor:'#70404a',marginBottom:12}}>{err}</div>}<form onSubmit={submit}><label className="label">Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label">Password<input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn primary" style={{width:'100%'}} disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="muted" style={{fontSize:12,marginTop:18}}>New here? <Link href="/register" style={{color:'var(--green)'}}>Create an account</Link></p></div></div></main>}

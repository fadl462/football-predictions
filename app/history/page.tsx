import {historyData} from '@/lib/data';
export default async function History(){const rows=await historyData(); const won=rows.filter((r:any)=>r.status==='WON').length, lost=rows.filter((r:any)=>r.status==='LOST').length, voids=rows.filter((r:any)=>r.status==='VOID').length; const rate=won+lost?Math.round(won/(won+lost)*100):0; return <main className="page"><div className="container"><span className="eyebrow">PUBLIC TRACK RECORD</span><h1>Proof, not promises.</h1><p className="muted">Every published demo/live prediction is retained with its outcome. The live system will keep the same audit trail.</p><div className="dashCards"><div className="dashCard"><span>Hit rate</span><b>{rate}%</b></div><div className="dashCard"><span>Won</span><b>{won}</b></div><div className="dashCard"><span>Lost</span><b>{lost}</b></div><div className="dashCard"><span>Void</span><b>{voids}</b></div></div><div className="historyTable" style={{marginTop:28}}><table><thead><tr><th>Date</th><th>Competition</th><th>Match</th><th>Prediction</th><th>Confidence</th><th>Result</th></tr></thead><tbody>{rows.map((r:any,i:number)=><tr key={i}><td>{r.date}</td><td>{r.league}</td><td>{r.match}</td><td>{r.prediction}</td><td>{r.confidence}%</td><td className={r.status==='WON'?'win':r.status==='LOST'?'loss':'void'}>{r.result}</td></tr>)}</tbody></table></div></div></main>}

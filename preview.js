const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);
$$('[data-scroll]').forEach(b=>b.addEventListener('click',()=>$(b.dataset.scroll)?.scrollIntoView({behavior:'smooth'})));
const menu=$('.mobile-menu');$('.mobile-toggle')?.addEventListener('click',()=>menu.classList.toggle('open'));$$('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
$$('.filter').forEach(b=>b.addEventListener('click',()=>{$$('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));
const modal=$('#modal'),content=$('#modal-content');
function show(type){if(type==='vip'){content.innerHTML='<span class="eyebrow gold">VIP / 30 JOURS</span><h3>Débloquez la sélection premium</h3><p>Dans la version live, ce parcours ouvre le checkout sécurisé Chargily Pay en DZD. Cet aperçu montre l’expérience utilisateur avant connexion des services.</p><div class="vip-stats"><div><b>1 999</b><span>DZD / 30 jours</span></div><div><b>08</b><span>picks max / jour</span></div></div><button class="gold-btn" onclick="alert(\'Démo : le paiement sera connecté après déploiement.\')">Continuer vers le paiement →</button>'}else{content.innerHTML='<span class="eyebrow">ESPACE MEMBRE</span><h3>Connexion</h3><p>Accédez à vos sélections VIP, votre statut d’abonnement et votre historique.</p><input class="modal-input" placeholder="Adresse e-mail"><input class="modal-input" type="password" placeholder="Mot de passe"><button class="primary">Se connecter</button><p style="margin-top:15px">Nouveau sur DZ Edge ? <b>Créer un compte</b></p>'}modal.classList.add('show')}
$$('[data-modal]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.modal)));$('.modal-close')?.addEventListener('click',()=>modal.classList.remove('show'));$('.modal-backdrop')?.addEventListener('click',()=>modal.classList.remove('show'));document.addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.remove('show')});

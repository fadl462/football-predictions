import {NextResponse} from 'next/server';import {getSessionUser} from '@/lib/auth';
export async function POST(){const u=await getSessionUser();if(!u)return NextResponse.json({error:'Sign in required'},{status:401});const key=process.env.PAYSTACK_SECRET_KEY,plan=process.env.PAYSTACK_VIP_PLAN_CODE;if(!key||!plan)return NextResponse.json({error:'Paystack is not configured yet. Add PAYSTACK_SECRET_KEY and PAYSTACK_VIP_PLAN_CODE.'},{status:503});const r=await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({email:u.email,plan,callback_url:`${process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000'}/vip`})});const j=await r.json();return NextResponse.json(j,{status:r.ok?200:502})}

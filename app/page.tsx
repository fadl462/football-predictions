import Link from 'next/link';import PredictionCard from '@/components/PredictionCard';import {homeData} from '@/lib/data';
export default async function Home(){const {matches,predictions}=await homeData();return <main><section className="hero"><div className="container"><span className="eyebrow">● Daily football intelligence</span><h1>Sharper picks.<br/><span>Smarter football.</span></h1><p>Automated football data, transparent prediction history and a premium VIP layer — built around a repeatable rules engine rather than manual daily updates.</p><div className="heroActions"><Link href="#today" className="btn primary">See today’s picks</Link><Link href="/pricing" className="btn">Unlock VIP</Link></div><div className="stats"><div className="stat"><strong>3</strong><span>Free picks daily</span></div><div className="stat"><strong>68%+</strong><span>Minimum confidence filter</span></div><div className="stat"><strong>24/7</strong><span>Automated data sync</span></div><div className="stat"><strong>100%</strong><span>Prediction history stored</span></div></div></div></section><section id="today" className="section"><div className="container"><div className="sectionHead"><div><h2>Today’s free predictions</h2><p>Automatically selected from available fixtures.</p></div><Link href="/history" className="btn">View history</Link></div><div className="cards">{predictions.map((p:any)=><PredictionCard key={p.id} p={p}/>)}</div></div></section><section className="section"><div className="container"><div className="sectionHead"><div><h2>VIP intelligence</h2><p>More markets, tighter filters and deeper daily coverage.</p></div><Link href="/pricing" className="btn gold">Get VIP</Link></div><div className="card"><div className="lock"><div><b>🔒 VIP predictions are subscriber-only</b><span className="muted">Create an account and activate VIP to see the full daily card.</span></div></div></div></div></section><section className="section"><div className="container"><div className="sectionHead"><div><h2>Matches today</h2><p>Live data is synchronized automatically from the football provider.</p></div><Link href="/matches" className="btn">All matches</Link></div><div className="fixtureList">{matches.slice(0,6).map((m:any)=><div className="fixture" key={m.id}><div className="league">{m.league}<br/><span>{m.country}</span></div><div className="teamsLine">{m.home} <span className="muted">vs</span> {m.away}</div><div><div className="kick">{new Date(m.kickoff).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</div><span className="status">{m.status==='NS'?'UPCOMING':m.status}</span></div></div>)}</div></div></section></main>}

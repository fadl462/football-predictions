import {historyData} from '@/lib/data';
export default async function History(){const rows=await historyData();return <main className="page"><div className="container"><span className="eyebrow">Transparent record</span><h1>Prediction history</h1><p className="muted">Every published pick is retained and settled after the match result is available.</p><div className="historyTable" style={{marginTop:28}}><table><thead><tr><th>Date</th><th>Match</th><th>Prediction</th><th>Confidence</th><th>Result</th></tr></thead><tbody>{rows.map((r:any,i:number)=><tr key={i}><td>{new Intl.DateTimeFormat('fr-DZ',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Africa/Algiers'}).format(new Date(r.date))}</td><td>{r.match}</td><td>{r.prediction}</td><td>{r.confidence}%</td><td className={r.status==='WON'?'win':r.status==='LOST'?'loss':'void'}>{r.result}</td></tr>)}</tbody></table></div></div></main>}
